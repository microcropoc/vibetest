# vt-30 — Info: schema URL с учётом base href

## Цель

Страница «Инфо» загружает `course.schema.json` на GitHub Pages (`/vibetest/`).

## Критерии готовности

- [ ] URL схемы через `document.baseURI`
- [ ] Тесты зелёные
